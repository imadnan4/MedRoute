# MedRoute — Heroku deploy dependencies (kept lean; no local-only ML/RAG stack).
# For local development with RAG + local Whisper, use requirements-dev.txt.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
sse-starlette==2.1.3

# LangChain agent framework
langchain==0.3.14
langchain-core==0.3.30
langchain-community==0.3.14

# HTTP calls to OpenRouter + Groq ASR
requests==2.32.3
httpx==0.28.1
PyJWT==2.10.1
cryptography==44.0.3
psycopg[binary]==3.2.3
numpy==1.26.4

# Report generation (system libs provided by Aptfile)
weasyprint==63.0
jinja2==3.1.5

# Roman Urdu transliteration (Groq ASR path)
uroman>=1.3.1.1
indic-transliteration>=2.3.0
